Bugs:

- [ ] The preview pane scrolls up to the top again whenever a background transcription finishes


Polish/refactor:

- [ ] An ID typed into go without a year should be smarter
    - if only one of them is in the current view, show that one
    - if multiple are in the current view, select the latest one


New features (likely):


New features (distant backlog):

- [ ] Integrations
    - [ ] rtappend
    - [ ] afui
    - [ ] email
    - [ ] dr
    - [ ] M2 entry

- [ ] Play by highlight timestamp?
    - Ideally you can do this in the editor as well as in the app.
    - It would be nice if play didn’t foreground mplayer so you can’t use the app










------

graves:

Let's change a couple of keybindings:

* copy -> y (for yank); use label "Copy Text"
* copy with timestamps -> Y; use label "Copy Text with Timestamps"
